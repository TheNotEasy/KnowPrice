import { SharedmodalDirective } from './sharedmodal.directive';

describe('SharedmodalDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedmodalDirective();
    expect(directive).toBeTruthy();
  });
});
