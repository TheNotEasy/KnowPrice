import { LoadableDirective } from './loadable.directive';

describe('LoadableDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadableDirective();
    expect(directive).toBeTruthy();
  });
});
